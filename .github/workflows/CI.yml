name: My Test Workflow

on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    
jobs: 
  ci:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
        uses: actions/checkout@v3

      - name: Use Node.js 
        uses: actions/setup-node@v4
        with: 
          node-version: '16x'
          
      - name: install npm 
        run: npm install

      - name: run lint
        
        

      
  cd:
      runs-on: ubuntu-latest
      steps: 
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Use Node.js 
        uses: actions/setup-node@v4
        with: 
          node-version: '16x'

       - name: install npm 
